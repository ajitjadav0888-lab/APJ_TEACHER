# APJ શિક્ષક — Android Project

Native Android WebView shell for the APJ Gujarati-first frontend.

## Build
Requires Android Studio / Android SDK and Gradle/Android Gradle Plugin 8.5.2.

`./gradlew assembleRelease`

The current environment did not contain a Gradle executable, so this project is prepared but an APK has not been falsely claimed as built.
